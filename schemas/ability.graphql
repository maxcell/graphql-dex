# What is an ability?
type Ability {
  # The unique identifier of this ability
  id: ID!

  # The name of this ability
  name: String!

  # A brief explanation of what the ability does
  short_effect: String!

  # Long description of what the ability does both in battle or in overworld
  effect: String!
}
